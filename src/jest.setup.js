    // jest.setup.js
    // Importa los matchers de jest-dom (ej. toBeInTheDocument) para usarlos en las pruebas
    import '@testing-library/jest-dom';

    // Aquí puedes añadir mocks globales si los necesitas, por ejemplo:
    // global.fetch = jest.fn(() => Promise.resolve({ json: () => Promise.resolve({}) }));
    